export enum BOTTOM_TAB_SCREENS {
  HOME = 'Home',
  SEARCH = 'Search',
  ORDERS = 'Orders',
  CART = 'Cart',
  PROFILE = 'Profile',
}

export enum ROOT_STACK_SCREENS {
  BOTTOM_TABS_NAVIGATOR = 'BottomTabNavigator',
}

export enum MODAL_STACK {
  EXAMPLE = 'Example',
}

export enum BOTTOM_SHEETS {
  EXAMPLE = 'Example',
}

export enum TRAY_SHEETS {
  EXAMPLE = 'Example',
}
