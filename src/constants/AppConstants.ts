export enum ENVIRONMENTS {
  DEV = 'development',
  PREVIEW = 'Preview',
  STAGING = 'staging',
  PRODUCTION = 'production',
}

export enum THUNK_RESPONSE_STATUS {
  SUCCESS = 'SUCCESS',
  FAIL = 'FAIL',
  CANCELLED = 'CANCELLED',
}

export enum COLOR_THEME {
  DARK = 'Dark',
  Light = 'Light',
  SYSTEM_DEFAULT = 'System Default',
}

export enum NetworkStatus {
  ONLINE = 'online',
  OFFLINE = 'offline',
}
