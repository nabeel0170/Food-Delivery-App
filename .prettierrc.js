module.exports = {
  arrowParens: 'always', // Always include parentheses around arrow function arguments
  bracketSameLine: true, // Keep closing bracket of JSX elements on the same line
  bracketSpacing: true, // Add spaces between brackets in object literals
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: 'all', // Include trailing commas wherever possible (e.g., in arrays, objects)
  semi: true, // Use semicolons at the end of statements
  jsxSingleQuote: true, // Use single quotes in JSX attributes
  printWidth: 80, // Max line length before wrapping (80 characters is the standard)
  tabWidth: 2, // Indentation with 2 spaces (common in JavaScript/TypeScript)
  useTabs: false, // Use spaces instead of tabs for indentation
  quoteProps: 'as-needed', // Only quote object properties when required (e.g., avoid quotes for simple keys)
  proseWrap: 'preserve', // Wrap markdown text as needed
  endOfLine: 'lf', // Use LF for newlines (UNIX style line breaks)
};
